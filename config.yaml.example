server:
  port: 8080
  # Requests per client IP per minute; set to 0 to disable.
  rate_limit_per_minute: 120

db:
  url: "postgres://localhost/skyn"

met:
  user_agent: "§"
  base_url: "https://api.met.no/weatherapi/locationforecast/2.0"

log:
  level: "info"
